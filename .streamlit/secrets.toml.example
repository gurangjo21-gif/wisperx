# 이 파일을 secrets.toml 로 복사하고 토큰을 채워서 로컬에서 사용하세요.
# Streamlit Community Cloud 에서는 "App settings → Secrets" 에 동일 내용을 붙여넣으세요.
# secrets.toml 은 .gitignore 에 등록되어 있어 절대 커밋되지 않습니다.

HF_TOKEN = "hf_여기에_본인_토큰_입력"
